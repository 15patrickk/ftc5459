#pragma config(Hubs,  S1, HTMotor,  HTMotor,  HTMotor,  HTServo)
#pragma config(Motor,  motorA,           ,             tmotorNXT, openLoop)
#pragma config(Motor,  motorB,           ,             tmotorNXT, openLoop)
#pragma config(Motor,  motorC,           ,             tmotorNXT, openLoop)
#pragma config(Motor,  mtr_S1_C1_1,     drivemotorR,   tmotorTetrix, openLoop)
#pragma config(Motor,  mtr_S1_C1_2,     drivemotorL,   tmotorTetrix, openLoop)
#pragma config(Motor,  mtr_S1_C2_1,     conveyormotor, tmotorTetrix, openLoop)
#pragma config(Motor,  mtr_S1_C2_2,     liftmotor,     tmotorTetrix, openLoop)
#pragma config(Motor,  mtr_S1_C3_1,     pullupmotorR,  tmotorTetrix, openLoop)
#pragma config(Motor,  mtr_S1_C3_2,     pullupmotorL,  tmotorTetrix, openLoop)
#pragma config(Servo,  srvo_S1_C4_1,    servo1,               tServoStandard)
#pragma config(Servo,  srvo_S1_C4_2,    servo2,               tServoNone)
#pragma config(Servo,  srvo_S1_C4_3,    servo3,               tServoNone)
#pragma config(Servo,  srvo_S1_C4_4,    servo4,               tServoNone)
#pragma config(Servo,  srvo_S1_C4_5,    servo5,               tServoNone)
#pragma config(Servo,  srvo_S1_C4_6,    servo6,               tServoNone)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//

#include "JoystickDriver.c"  //Include file to "handle" the Bluetooth messages.

// initializeRobot() - perform initialization routines. first routine called by main().
void initializeRobot() {
  // Place code here to initialize servos to starting positions.
  // Sensors are automatically configured and set up by ROBOTC. They may need a brief time to stabilize.

  return;
}

// main() - the main task for the robot. this is basically an infinite loop.
// DO NOT MODIFY unless you've been cleared for it/know what you're doing.
// this must be the last routine in the file.

// Game controller information is sent every ~50 milliseconds from the FMS (Field Management System).
// At the end of the tele-op period, the FMS will autonmatically halt execution.
task main() {
  initializeRobot();
  waitForStart(); // wait for start of tele-op phase

  while(true) {
	  // Insert code to have servos and motors respond to joystick and button values.
    // Look in the ROBOTC samples folder for code that may be adaptable to the problem at hand.
  }
}
